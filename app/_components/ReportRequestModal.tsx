"use client";

import { ReportPdfDownloadModal } from "./ReportPdfDownloadModal";

/** Hero CTA — latest edition PDF (placeholder file until a new asset is provided). */
export function ReportRequestModal() {
  return <ReportPdfDownloadModal edition="latest" variant="hero-cta" />;
}
